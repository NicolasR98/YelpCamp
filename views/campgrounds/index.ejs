<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YelpCamp Campgrounds</title>
  </head>
  <body>
    <h1>YelpCamp: All Campgrounds</h1>
    <a href="/campgrounds/new">Add a new campground</a>
    <ul>
      <% for(let campground of campgrounds){ %>
      <li><a href="/campgrounds/<%= campground._id %> "><b><%= campground.title %></b> - <%= campground.location %></a></li>
      <% } %>
    </ul>
  </body>
</html>
